# Session state. Read this first; `survey-context` does it for you.
#
# workflow_mode is the canonical integrate-mode signal that `release-branch`
# reads. It is `team-pr`, not the scaffold default of `solo-git`, because this
# repo never commits to main: every change goes issue, branch, PR, squash merge.
# See AGENTS.md, "Commits, PRs, CHANGELOG".
workflow_mode: team-pr

active_phase: sustain
active_flow: release_branch


metrics:
  story_start: "2026-09-10T23:12:45Z"


project:
  name: vixygrey-dev-setup
  description: >-
    A single idempotent Bash script that provisions a macOS developer machine and
    generates the user's OMP environment.

status:
  summary: >-
    Issue #589 is implemented and verified on feature/replace-zed-with-kiro.
    Kiro replaces Zed with a local Dracula-Sakura theme and merged editor defaults.

handoff:
  next_skill: release-branch
